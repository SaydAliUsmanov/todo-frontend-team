const initialState = {
  todos: [],
  loading: false,
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    // case '':
    //   return
    default:
      return state
  }
}